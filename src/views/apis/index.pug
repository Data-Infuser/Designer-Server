extends ../layout

block content
  ol.breadcrumb
    li.breadcrumb-item.active 
        a(href="/apis") API 관리
  .card.mb-3
    .card-header
      i.fas.fa-table
        |             API 목록

    .card-body
      a.mb-3.btn.btn-primary(href="/apis/new") 파일 업로드
      .table-responsive
        table.abnftable.table-bordered(width='100%' cellspacing='0')
          thead
              tr  
                th API명
          tbody
            each api in apis
              tr
                td #{api.title}
